/*
 * -@TestCaseID: override_0015.cj 
 * -@TestCaseName: override_0015.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1024-05
 * -@RequirementName: Record 主构造函数的语法支持
 * -@Condition: no
 * -@Brief: 一个主构造函数，一个构造函数，泛型参数一样，其他参数不一样但有默认值，不构成重载冲突，但调用时无法决议。
 * -#step1: 一个主构造函数，一个构造函数，泛型参数一样，其他参数不一样但有默认值，不构成重载冲突，但调用时无法决议。
 * -@Expect: 构造实例时执行成功，调用主构造器，返回期望结果
 * -@Priority: High
 */
