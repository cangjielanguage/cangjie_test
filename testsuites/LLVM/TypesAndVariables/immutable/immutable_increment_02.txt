/*
 * -@TestCaseID: immutable_increment_02.cj
 * -@TestCaseName: immutable_increment_02.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1015-20
 * -@RequirementName: 表达式计算得到的值需要做immutable检查
 * -@Condition: no
 * -@Brief: var/let的变量类型为recrod/class，不可访问immutable的成员变量，不能访问let的record的var变量。 
 * -#step1: var/let的变量类型为recrod/class，不可访问immutable的成员变量，不能访问let的record的var变量。
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.4
 * -@SpecChapter: 
 * -@SpecDetail: 
 * -@catalog:# 名字、作用域、变量## 变量### 变量的初始化#### 不可变变量和可变变量--3 
 */
