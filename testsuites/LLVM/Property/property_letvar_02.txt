/*
 * -@TestCaseID: property_letvar_02.cj
 * -@TestCaseName: property_letvar_02.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1022-30
 * -@RequirementName: 类中支持属性
 * -@Condition: no
 * -@Brief: 使用 let 声明的属性定义 setter 实现。使用 var 声明的属性缺少定义 getter（对应取值） 或 setter（对应赋值）
 * -#step1: 使用 let 声明的属性定义 setter 实现。使用 var 声明的属性缺少定义 getter（对应取值） 或 setter（对应赋值）
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec0.22
 * -@SpecChapter: 属性/属性的修饰符
 * -@SpecDetail: 使用 let 声明的属性需要定义 getter（对应取值） 实现。使用 var 声明的属性需要分别定义 getter（对应取值） 和 setter（对应赋值） 的实现
 */