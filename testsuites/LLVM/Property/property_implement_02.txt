/*
 * -@TestCaseID: property_implement_02.cj
 * -@TestCaseName: property_implement_02.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1022-30
 * -@RequirementName: 类中支持属性
 * -@Condition: no
 * -@Brief: 属性的实现同函数的实现规则一样，其中可以包含声明和表达式，可以省略 return ，其返回值必须符 合返回类型。
 * -#step1: 属性的实现同函数的实现规则一样，其中可以包含声明和表达式，可以省略 return ，其返回值符 合返回类型。
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec0.22
 * -@SpecChapter: 属性/属性的修饰符
 * -@SpecDetail: 属性的实现同函数的实现规则一样，其中可以包含声明和表达式，可以省略 return ，其返回值必须符 合返回类型。
 */