/*
 * -@TestCaseID: class_interface_27n.cj
 * -@TestCaseName: class_interface_27n.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1003-01
 * -@RequirementName: 支持类和对象，支持构造和析构、函数、属性、嵌套类、接口、继承、覆盖、重载。
 * -@Condition: no
 * -@Brief: 测试同时继承一个类，实现一个接口，实现接口时未指明接口名
 * -#step1: 定义一个open修饰的基类Basec，类中声明一个变量
 * -#step2: 定义一个接口Basei，接口中声明一个默认函数，返回数值
 * -#step3: 定义Data类使用extends继承Basec,implements关键字实现接口，但是缺失接口名
 * -@Expect: 缺失接口名，编译器报错
 * -@Priority: High
 */