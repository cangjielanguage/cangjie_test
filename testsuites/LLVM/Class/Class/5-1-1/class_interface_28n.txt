/*
 * -@TestCaseID: class_interface_28n.cj
 * -@TestCaseName: class_interface_28n.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1003-01
 * -@RequirementName: 支持类和对象，支持构造和析构、函数、属性、嵌套类、接口、继承、覆盖、重载。
 * -@Condition: no
 * -@Brief: 测试先实现接口再继承类，编译器报错
 * -#step1: 定义一个open修饰的基类Basec，类中声明一个变量
 * -#step2: 定义一个接口Basei，接口中声明一个默认函数，返回数值
 * -#step3: 定义Data类先使用implements关键字实现Basei，再使用extends继承Basec
 * -@Expect: 先实现接口再继承类，编译器报错
 * -@Priority: High
 */