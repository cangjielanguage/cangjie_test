/*
 * -@TestCaseID: extends_override_rule_09n.cj
 * -@TestCaseName: extends_override_rule_09n.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1003-01
 * -@RequirementName: 支持类和对象，支持构造和析构、函数、属性、嵌套类、接口、继承、覆盖、重载。
 * -@Condition: no
 * -@Brief:子类用override重写父类默认（public）函数,父类函数入参为YY，子类函数入参为YY的父类YY，编译报错
 * -#step1:创建父类Base，有默认（public）函数foo,有1个入参,入参是类YY,YY是XX的子类
 * -#step2:创建子类Data继承Base，写函数foo,入参是XX,返回类型相同
 * -#step3:检查编译报错信息
 * -@Expect:编译报错，此场景不是“覆盖”场景，不应该用override
 * -@Priority: High
 */
