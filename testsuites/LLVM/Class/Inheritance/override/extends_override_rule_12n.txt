/*
 * -@TestCaseID: extends_override_rule_12n.cj
 * -@TestCaseName: extends_override_rule_12n.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1003-01
 * -@RequirementName: 支持类和对象，支持构造和析构、函数、属性、嵌套类、接口、继承、覆盖、重载。
 * -@Condition: no
 * -@Brief:子类无override写父类默认（public）函数,父类返回类型为XX，子类返回类型为XX的子类YY，编译报错
 * -#step1:创建父类Base，有默认（public）函数foo,有1个入参,返回类型是类XX
 * -#step2:创建子类Data继承Base，无override写函数foo,返回类型是XX的子类YY,入参相同
 * -#step3:检查编译报错信息
 * -@Expect:编译报错，此场景不是“覆盖”场景，不应该用override
 * -@Priority: High
 */
