/*
 * -@TestCaseID: extends_override_rule_04n.cj
 * -@TestCaseName: extends_override_rule_04n.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-1003-01
 * -@RequirementName: 支持类和对象，支持构造和析构、函数、属性、嵌套类、接口、继承、覆盖、重载。
 * -@Condition: no
 * -@Brief:子类无override成功重写父类默认（public）含1个入参的函数，编译报错
 * -#step1:创建父类Base，有默认（public）函数foo,有1个入参
 * -#step2:创建子类Data继承Base，无override重写函数foo,有相同的入参,返回类型相同
 * -#step3:检查编译信息
 * -@Expect:编译报错，符合“覆盖”条件，但是没有override
 * -@Priority: High
 */
