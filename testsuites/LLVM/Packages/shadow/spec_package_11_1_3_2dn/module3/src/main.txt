/*
 * -@TestCaseID: main.cj
 * -@TestCaseName: main.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 使用import as 对导入的声明进行重命名后，当前文件只能使用重命名后的新名字，原名无法直接使用。
 * -#step1: 使用import as 对导入的声明进行重命名后，当前文件只能使用重命名后的新名字，原名无法直接使用。
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 使用import as 对导入的声明进行重命名后，当前文件只能使用重命名后的新名字，原名无法直接使用。
 */