/*
 * -@TestCaseID: default.cj
 * -@TestCaseName: default.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 仓颉支持import packageName.name as newName 的方式对顶层声明进行重命名。在导入不同包的同名顶级声明时，可以使用重命名来避免名字的冲突
 * -#step1: 仓颉支持import packageName.name as newName 的方式对顶层声明进行重命名。在导入不同包的同名顶级声明时，可以使用重命名来避免名字的冲突
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 仓颉支持import packageName.name as newName 的方式对顶层声明进行重命名。在导入不同包的同名顶级声明时，可以使用重命名来避免名字的冲突
 */