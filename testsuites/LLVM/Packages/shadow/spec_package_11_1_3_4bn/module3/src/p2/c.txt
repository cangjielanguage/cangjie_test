/*
 * -@TestCaseID: c.cj
 * -@TestCaseName: c.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明，而不能通过packageName.declName 或declName 的方式，因为这两种方式均无法区分使用的是哪个定义或声明。
 * -#step1: 但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明，而不能通过packageName.declName 或declName 的方式，因为这两种方式均无法区分使用的是哪个定义或声明。
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明，而不能通过packageName.declName 或declName 的方式，因为这两种方式均无法区分使用的是哪个定义或声明。
 */