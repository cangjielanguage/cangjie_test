/*
 * -@TestCaseID: default.cj
 * -@TestCaseName: default.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 支持import pkg.* as newPkgName.* 的形式对包名进行重命名，以解决不同模块中同名包的名字冲突问题
 * -#step1: 支持import pkg.* as newPkgName.* 的形式对包名进行重命名，以解决不同模块中同名包的名字冲突问题
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 支持import pkg.* as newPkgName.* 的形式对包名进行重命名，以解决不同模块中同名包的名字冲突问题
 */