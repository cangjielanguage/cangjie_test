/*
 * -@TestCaseID: default.cj
 * -@TestCaseName: default.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 导入另外一个module的default包(包名pkgDefault)，以package（默认）的方式编译，能成功编译
 * -#step1: 导入另外一个module的default包(包名pkgDefault),以package（默认）的方式编译，能成功编译
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 如果模块采用生成可执行文件的编译方式（由用户指定），则编译器只在源文件根目录下的顶层查找main 函数（不考虑导入的）。
 */
