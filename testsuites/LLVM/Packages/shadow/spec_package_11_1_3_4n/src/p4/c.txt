/*
 * -@TestCaseID: c.cj
 * -@TestCaseName: c.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 如果没有重命名，并导入了不同包中的同名的顶层声明，在import 导入声明处不报错。在使用处，只能使用packageName.xxx 访问存在冲突的内容，仅使用顶层声明的名字来访问将会报错。
 * -#step1: 如果没有重命名，并导入了不同包中的同名的顶层声明，在import 导入声明处不报错。在使用处，只能使用packageName.xxx 访问存在冲突的内容，仅使用顶层声明的名字来访问将会报错。
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 如果没有重命名，并导入了不同包中的同名的顶层声明，在import 导入声明处不报错。在使用处，只能使用packageName.xxx 访问存在冲突的内容，仅使用顶层声明的名字来访问将会报错。
 */