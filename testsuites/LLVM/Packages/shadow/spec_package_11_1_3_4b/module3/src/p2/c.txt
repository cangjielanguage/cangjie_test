/*
 * -@TestCaseID: c.cj
 * -@TestCaseName: c.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1033-10
 * -@RequirementName: 包导入导出规则改动
 * -@Condition: no
 * -@Brief: 如果从不同模块中导入同名包中相同名称的定义或声明，在没有重命名的情况下，导入处并不会报错，但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明
 * -#step1: 如果从不同模块中导入同名包中相同名称的定义或声明，在没有重命名的情况下，导入处并不会报错，但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.2
 * -@SpecChapter: 包和模块管理/包
 * -@SpecDetail: 如果从不同模块中导入同名包中相同名称的定义或声明，在没有重命名的情况下，导入处并不会报错，但在使用处只能通过moduleName.packageName.declName 的方式访问存在的冲突的定义或声明
 */