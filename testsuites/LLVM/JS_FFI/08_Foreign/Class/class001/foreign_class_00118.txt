 /*
 * -@TestCaseID: Foreign_class_00118
 * -@TestCaseName: foreign_class_00118.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-2031-01
 * -@RequirementName: Foreign Function
 * -@Conditon: cjc, clang, maple test framework and python3.
 * -@Brief: Define init without body inside foreign class.
 * -@Expect: Compile fail, return 1.
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.6
 * -@SpecChapter: foreign 类和接口
 * -@SpecDetail: 1、`foreign` 修饰符支持修饰顶级类、接口、函数以及变量（ `var` 和 `let` ）
 * -@SpecDetail: 2、`foreign` 块语句并不引入新的作用域，它与 `foreign` 单个语句的作用完全相同 
 * -@SpecDetail: 3、不能有 `init` 构造函数，不能有 `init block`，不允许实例化（实际对象只能从 `JavaScript` 侧创建并返回）
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符--1
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符--4
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符##### foreign 类和接口--8
 */
