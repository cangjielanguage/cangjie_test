 /*
 * -@TestCaseID: Foreign_class_00126
 * -@TestCaseName: foreign_class_00126.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-ARKLANG-2031-01
 * -@RequirementName: Foreign Fuction
 * -@Conditon: cjc, clang, maple test framework and python3.
 * -@Brief: Define member variable(let\var) inside foreign class.
 * -@Expect: Compile success, return 0.
 * -@Priority: level0
  -@SpecVersion: spec 0.22.6
 * -@SpecChapter: foreign 类和接口
 * -@SpecDetail: 1、`foreign` 修饰符支持修饰顶级类、接口、函数以及变量（ `var` 和 `let` ）2、 成员变量为 `let` 时不要求必须初始化
 * -@SpecDetail: 3、在仓颉中使用 `foreign` 修饰 class 来表示外部 `JavaScript` 类，其成员都默认为 `foreign`，且无需再添加 `foreign` 修饰符。
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符--1
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符##### foreign 类和接口--1
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### foreign 修饰符##### foreign 类和接口--6
 */
