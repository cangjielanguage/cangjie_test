 /*
 * -@TestCaseID: JsMod_import_var_19
 * -@TestCaseName: JsMod_import_var_19.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: SR-Lang-1005
 * -@RequirementName: JS互操作
 * -@Conditon: cjc, clang, maple test framework and python3.
 * -@Brief: 仓颉通过jsModule调用js中var定义的number类型的变量
 * -#step1: js侧var定义全局变量，变量类型number,作为成员导出
 * -#step2: 仓颉侧@JsModule修饰var变量，变量类型为为enum
 * -#step3: step2中的变量在main中调用
 * -@Expect: 编译通过.
 * -@Priority: level0
 * -@SpecVersion: spec 0.22.6
 * -@SpecChapter: @JsModule
 * -@SpecDetail: 1、仓颉侧enum映射Js侧number 2、当 `JavaScript` 包直接导出函数或变量时，可以通过 `@JsModule` 注解来表示
 * -@SpecDetail: 3、通过 `foreign` 修饰符以及一系列注解（`@JsModule`，`@JsMember`，`@JsRename`）来书写绑定。
 * -@SpecDetail: 4、`enum` 类型仅当所有的 `constructor` 都只有一个类型参数时支持直接作为具体的类型参数进行映射
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript#### @JsModule--1
 * -@catalog: # 语言互操作## JavaScript 互操作### 类型映射#### 值类型映射--1
 * -@catalog: # 语言互操作## JavaScript 互操作### 仓颉调用 JavaScript--5
 * -@catalog: # 语言互操作## JavaScript 互操作### 类型映射#### 值类型映射--3
 */
