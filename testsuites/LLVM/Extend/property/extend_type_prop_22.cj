// EXEC-PIPE-1: %compiler %cmp_opt %n.cj -o %output 2>&1 | compare %f
// ASSERT: regex-not core dump

func main() : Int64 {
    var a:Int32*Int64 = (1,2)
    return 0
}

extend Int32*Int64 { // EXPECTED: regex-auto extending type 'Tuple<Int32, Int64>' is not allowe
    prop var m: Int64 {
        get() {
            return 2
        }
        set(v){}
    }
    prop let n: Int64 {
        get() {
            return 1
        }
    }
}