/*
 * -@TestCaseID: operator_conflict_03.cj
 * -@TestCaseName: operator_conflict_03.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1010-07
 * -@RequirementName: 使用extend来添加该类型的操作符函数
 * -@Condition: no
 * -@Brief: 扩展In32类型的==操作符,重载操作符与内置操作符签名不同,不明确入参类型,通过类型推断确定入参,候选集有多个函数,优先执行builtin的操作符
 * -#step1: extend类型Int32,重载操作符==,定义两个操作符函数,函数一参数为Char类型返回值为Bool类型，函数二参数为Int64类型返回值为String类型
 * -#step2: 调用==时,入参为数值1
 * -@Expect: 优先执行builtin的操作符,运行成功
 * -@Priority: level0
 * -@SpecVersion: spec0.22.1
 * -@SpecChapter: 操作符重载
 * -@SpecDetail: 使用extend的方式为其添加操作符函数，从而实现操作符在这些类型上的重载。对于无法直接包含函数定义的类型（是指除 record 、 class 、 enum 和 interface 之外其他的类型），只能采用这
种方式；对于可以直接包含函数定义的类型(包括class、interface、enum 和record)，可以直接在其内部定义操作符函数的方式实现操作符的重载。
 */