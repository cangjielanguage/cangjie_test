/*
 * -@TestCaseID: a_top_enum_02.cj
 * -@TestCaseName: a_top_enum_02.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1015-14
 * -@RequirementName: 函数重载决议
 * -@Condition: no
 * -@Brief: 作用域级别无差别：top、enum(省略前缀)的决议，候选集含有enum的foo，则无法决议
 * -#step1: 作用域级别相同的情况下，top、enum(省略前缀)的决议，候选集含有enum的foo，则编译报错
 * -@Expect: 输出正确编译报错信息
 * -@Priority: level0
 * -@SpecVersion: spec0.22
 * -@SpecChapter: 重载/函数重载/重载函数候选集/可见函数集
 * -@SpecDetail: 不带限定前缀直接通过名字调用 f(...) ，可见函数集包含以下几种方式引入的名为 f 的函数:作用域中可见的局部函数
 */