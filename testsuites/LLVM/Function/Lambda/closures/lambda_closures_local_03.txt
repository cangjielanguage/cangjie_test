/*
 * -@TestCaseID: lambda_closures_local_03.cj
 * -@TestCaseName: lambda_closures_local_03.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1016-04
 * -@RequirementName: lambda 尾闭包
 * -@Condition: no
 * -@Brief: 仅捕获了let声明的局部变量的函数或lambda可以作为一等公民使用，即可以赋值给变量，可以作为实参或返回值使用。
 * -#step1: 仅捕获了let声明的局部变量的函数或lambda可以作为一等公民使用，即可以赋值给变量，可以作为实参或返回值使用。
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec0.22
 * -@SpecChapter: 函数/闭包/
 * -@SpecDetail: 仅捕获了let声明的局部变量的函数或lambda可以作为一等公民使用，即可以赋值给变量，可以作为实参或返回值使用。
 */