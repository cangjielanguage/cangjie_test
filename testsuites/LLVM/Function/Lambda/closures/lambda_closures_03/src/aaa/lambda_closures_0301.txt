/*
 * -@TestCaseID: lambda_closures_0301.cj
 * -@TestCaseName: lambda_closures_0301.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1016-04
 * -@RequirementName: lambda 尾闭包
 * -@Condition: no
 * -@Brief: 即使对闭包调用不在定义的作用域，仍可访问其捕获的局部变量
 * -#step1: 即使对闭包调用不在定义的作用域，仍可访问其捕获的局部变量
 * -@Expect: 编译执行成功、返回0
 * -@Priority: level0
 * -@SpecVersion: spec0.22
 * -@SpecChapter: 函数/闭包/
 * -@SpecDetail: 即使对闭包调用不在定义的作用域，仍可访问其捕获的局部变量
 */