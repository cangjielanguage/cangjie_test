/*
 * -@TestCaseID: matchpattern_tuple_11.cj
 * -@TestCaseName: matchpattern_tuple_11.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: SR-Lang-1019
 * -@RequirementName: 模式匹配
 * -@Condition: no
 * -@Brief: 对于 tuple pattern 中引入的变量，其作用域从变量第一次出现的位置处开始至下一个 `case` 之前(x1可见不可用)
 * -#step1: 对于 tuple pattern 中引入的变量，其作用域从变量第一次出现的位置处开始至下一个 `case` 之前(x1可见不可用)
 * -@Expect: 编译报错
 * -@Priority: level0
 * -@SpecVersion: spec0.22.6
 * -@SpecChapter: 表达式/模式匹配表达式/序列模式
 * -@SpecDetail: 需要注意的是：对于 tuple pattern 中引入的变量，其作用域从变量第一次出现的位置处开始至下一个 `case` 之前。并且此条规则同样适用于下面将介绍的 enum 模式。
 * -@catalog:# 表达式## 模式匹配表达式### 模式#### Tuple 模式--6
 */