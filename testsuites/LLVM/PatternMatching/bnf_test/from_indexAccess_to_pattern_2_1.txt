
 /*
 * -@TestCaseID: xxx
 * -@TestCaseName: xxx
 * -@TestCaseType: Function Test
 * -@RequirementID: RequirementID
 * -@RequirementName: pattern
 * -@Conditon: cjc, clang, maple test framework and python3.
 * -@Brief: Test <pattern> in <indexAccess> case (use rangeElement):
Origin BNF is 
    :  LSQUARE NL* ( (expression (NL* COMMA NL* expression)*) | rangeElement) NL* RSQUARE
    
Test   BNF is 
    :  LSQUARE NL* ( (expression (NL* COMMA NL* expression)*) | <<TARGET=rangeElement->pattern>>) NL* RSQUARE
     
。
 * -@Expect: see case.
 * -@Priority: level0
 */
