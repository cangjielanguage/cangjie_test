
 /*
 * -@TestCaseID: xxx
 * -@TestCaseName: xxx
 * -@TestCaseType: Function Test
 * -@RequirementID: RequirementID
 * -@RequirementName: pattern
 * -@Conditon: cjc, clang, maple test framework and python3.
 * -@Brief: Test <pattern> in <classPrimaryInit> case (use callSuffix):
Origin BNF is 
    :  (classNonStaticMemberModifier NL*)?  className NL* LPAREN NL*  classPrimaryInitParamLists? NL* RPAREN NL*
 	  LCURL NL*
 	       (SUPER callSuffix)? end
 	       expressionOrDeclarations?
 	  NL* RCURL
	
Test   BNF is 
    :  (classNonStaticMemberModifier NL*)?  className NL* LPAREN NL*  classPrimaryInitParamLists? NL* RPAREN NL*
 	  LCURL NL*
 	       (SUPER <<TARGET=callSuffix->pattern>>)? end
 	       expressionOrDeclarations?
 	  NL* RCURL
	 
。
 * -@Expect: see case.
 * -@Priority: level0
 */
