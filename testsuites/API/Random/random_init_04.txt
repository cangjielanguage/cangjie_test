/*
 * -@TestCaseID: random_init_04.cj
 * -@TestCaseName: random_init_04.cj
 * -@TestCaseType: Function Test
 * -@RequirementID: AR-SR-Lang-1097-02
 * -@RequirementName: 提供 class Random
 * -@Condition: no
 * -@Brief: 多个生成器调用同一函数生成随机数，调用次数不同，生成的随机数值不可能次次相同，多次中至少存在一次不同即为正确
 * -#step1: 多个生成器调用同一函数生成随机数，调用次数不同，生成的随机数值不可能次次相同，多次中至少存在一次不同即为正确
 * -@Expect: 编译执行成功返回0
 * -@Priority: level0
 * -@SpecVersion: api doc
 * -@SpecChapter: Random
 * -@SpecDetail: init()
 */
