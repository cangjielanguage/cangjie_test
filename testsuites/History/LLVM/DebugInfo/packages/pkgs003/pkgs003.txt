/*
 * -@TestCaseID: pkgs003.cj
 * -@TestCaseName: pkgs003.cj
 * -@TestCaseType: Function Test
 * -@RequirementID:
 * -@RequirementName: 支持import语法:
    双文件夹内各单文件、变量、函数、
    import pkgs1.varpkg1_001, pkgs2.varpkg1_001
    import pkgs1.funpkg1001 as f1, pkgs2.funpkg1001 as f2
 * -@Condition: no
 * -@Brief:import external变量并访问
 * -#step1:import external变量并访问
 * -#step2:检查编译执行结果
 * -@Expect:执行正确
 * -@Priority: High
 */
