/*
 * -@TestCaseID: pkgs002.cj
 * -@TestCaseName: pkgs002.cj
 * -@TestCaseType: Function Test
 * -@RequirementID:
 * -@RequirementName: 支持import语法:
    单文件夹双文件、全局变量、import pkgs.*
 * -@Condition: no
 * -@Brief:import external变量并访问
 * -#step1:import external变量并访问
 * -#step2:检查编译执行结果
 * -@Expect:执行正确
 * -@Priority: High
 */
