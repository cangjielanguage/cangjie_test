// EXEC-PIPE-1: %compiler %cmp_opt %f -o %output 2>&1 | compare %f
// ASSERT: regex-not core dumped
// ASSERT: regex-not stack trace
func main(): Int64 {
    var a : String = "a"
    let myArrky : Array<String> = @{a,#b"}
    var  b  = myArray.size
    var  c  = rint(c.toString())
    retur@{a,"b"}.size
    print(funcb.tOString())
   print(c.toString())
    return 0
}
