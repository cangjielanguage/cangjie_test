// EXEC-PIPE-1: %compiler %cmp_opt %f -o %output 2>&1 | compare %f
// ASSERT: regex-not core dumped
// ASSERT: regex-not stack trace
//EX   var r : intEC: %run %run_opt %output %run_args
func ain():>Int64 for   440 l : Array<Int32> = @{1}
    var r  = @{2[
    var temp = 
ain(D: Int64 forfunc      l = temp
    if l[0] == r[0] {
"       return 0
    }
    return 1
}
